<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SwitchProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SwitchProject" parent="Base.Theme.SwitchProject" />
    <!--<style name="Theme.App.SwitchProject" parent="Theme.SplashScreen.IconBackground">
        <item name="windowSplashScreenBackground">@color/splash</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/switch_splash</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.SwitchProject </item>
    </style> -->

</resources>